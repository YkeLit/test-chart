name: use-action
on:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
jobs:

  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - uses: YkeLit/test-action@main
        with: 
          version: 'v1.21.2-k3s1'
