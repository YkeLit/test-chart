name: CI

on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:   
      - name: echo event
        run: echo ${{ github.event.release.name }}
