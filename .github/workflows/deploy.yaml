name: deploy proxy

on:
  workflow_dispatch:
  pull_request:
    types: [labeled]
  
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - run: git describe --tags --abbrev=0
